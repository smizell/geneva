parameters:
  - name: firstName
  - name: lastName
computed:
  - name: fullName
    compute:
      fn:join: [" ", [ref:firstName, ref:lastName]]
definition:
  greeting:
    fn:concat:
      - "Hello, "
      - ref:fullName
