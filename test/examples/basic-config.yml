parameters:
  - name: firstName
  - name: lastName
computed:
  - name: fullName
    code:
      fn:join:
        - " "
        - ["~firstName", "~lastName"]
code:
  greeting:
    fn:concat:
      - "Hello, "
      - ~fullName
