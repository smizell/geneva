parameters:
  - name: firstName
    check: ref:isString
  - name: lastName
    required: true
computed:
  - name: fullName
    compute:
      fn:join: [" ", [ref:firstName, ref:lastName]]
definition:
  greeting:
    fn:concat:
      - "Hello, "
      - ref:fullName
