parameters:
  - name: firstName
    check: ref:isString
  - name: lastName
computed:
  - name: fullName
    compute:
      fn:join: [" ", [ref:firstName, ref:lastName]]
definition:
  greeting:
    fn:concat:
      - "Hello, "
      - ref:fullName
