parameters:
  - name: firstName
    type: isString
  - name: lastName
    type: isString
computed:
  - name: fullName
    code:
      fn:join:
        - " "
        - ["~firstName", "~lastName"]
code:
  greeting:
    fn:concat:
      - "Hello, "
      - ~fullName
